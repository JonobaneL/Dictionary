import styles from "../../assets/styles/UI/VerificationMessage.module.scss";

const VerificationMessage = () => {
  return (
    <div className={styles.message}>
      Verify your email to access all features. Thank you!
    </div>
  );
};

export default VerificationMessage;
